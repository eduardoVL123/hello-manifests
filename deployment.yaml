apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-app-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-app
  template:
    metadata:
      labels:
        app: hello-app
    spec:
      containers:
      - name: hello-app
        image: eduardolvalerio/hello-app:20bcd52
        ports:
        - containerPort: 80